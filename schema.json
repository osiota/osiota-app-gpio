{
	"type": "object",
	"title": "osiota application GPIO",
	"properties": {
		"pin": {
			"type": "number",
			"title": "GPIO pin number"
		},
		"invert": {
			"type": "boolean",
			"title": "Invert value",
			"description": "e.g. for a pull down circuit"
		},
		"map": {
			"type": "array",
			"format": "table",
			"title": "Actions",
			"items": {
				"type": "object",
				"title": "Action",
				"properties": {
					"type": {
						"type": "string",
						"enum": ["switch", "press", "longpress"]
					},
					"node": {
						"type": "string"
					}
				},
				"required": ["type", "node"]
			}
		}
	},
	"additionalProperties": false
}
